package com.example
import akka.actor._
import com.github.jodersky.flow._
import akka.io.IO

object Hello {
  def main(args: Array[String]): Unit = {
    println("Hello, world!")
    

    //IO(Serial) ! Serial.Open(port, settings )
    
  }
}
